const express = require('express');
const router = express.Router();

// Placeholder para rutas de contratos
router.get('/', (req, res) => {
  res.json({ message: 'Rutas de contratos - En desarrollo' });
});

module.exports = router;