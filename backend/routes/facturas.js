const express = require('express');
const router = express.Router();

// Placeholder para rutas de facturas
router.get('/', (req, res) => {
  res.json({ message: 'Rutas de facturas - En desarrollo' });
});

module.exports = router;